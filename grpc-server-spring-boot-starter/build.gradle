plugins {
    id "java"
    id "java-library"
}

apply from: '../deploy.gradle'

group = "net.devh"
version = projectVersion

dependencies {
    api project(':grpc-server-spring-boot-autoconfigure')
}
