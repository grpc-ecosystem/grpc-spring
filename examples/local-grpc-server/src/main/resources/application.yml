spring:
  application:
    name: local-grpc-server
grpc:
  server:
    port: 9898

management:
  endpoint:
    metrics:
      enabled: true
  endpoints:
    web:
      exposure:
        include: ["prometheus", "metrics"]
